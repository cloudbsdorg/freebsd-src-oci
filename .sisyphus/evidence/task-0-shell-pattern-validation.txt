T0 Area 1 - shell pattern validation (POSIX, FreeBSD 16 host)
Host: pppoe1.cloudbsd.org (FreeBSD 16.0-CURRENT amd64, 6 CPUs)
Date: 2026-06-08

PASS items:
  - /bin/sh is FreeBSD sh (POSIX 2008 + extensions)
  - sh -c 'set -eu; echo ok' succeeds
  - mktemp -d -t template-test-XXXXXX works (FreeBSD syntax)
  - openssl req -x509 -newkey rsa:2048 -nodes ... works (1123-byte cert, 1704-byte key)
  - openssl pkcs12 -export ... works (2547-byte bundle)
  - kyua is installed at /usr/bin/kyua
  - pw useradd SYNTAX is correct (root-only execution; mlapointe is non-root)

FAIL / deferred:
  - tests/sys/env/verify_test_env.sh does not exist (T0.A follow-up)
  - tests/data/regenerate.sh does not exist (T0.B follow-up)
  - tests/data/users/setup-test-users.sh does not exist (T0.C follow-up)
  - kldload / kldunload require root (T0.D follow-up)
  - kyua --version / kyua test --help wrong on this kyua version (use 'kyua help' instead)
  - local var=val in single-line sh -c is silently a no-op (use 2-line form)
